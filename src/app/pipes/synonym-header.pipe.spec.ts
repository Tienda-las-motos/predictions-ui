import { SynonymHeaderPipe } from './synonym-header.pipe';

describe('SynonymHeaderPipe', () => {
  it('create an instance', () => {
    const pipe = new SynonymHeaderPipe();
    expect(pipe).toBeTruthy();
  });
});
